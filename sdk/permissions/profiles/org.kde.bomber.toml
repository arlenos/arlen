# Starting permission profile for Bomber (a KDE arcade game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access. Identity only - the maximally fail-closed baseline.
[info]
app_id = "org.kde.bomber"
tier = "third-party"
