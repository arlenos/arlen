# Starting permission profile for Notesnook (encrypted notes with sync).
# Syncs notes over the network by function; notes live in its own config dir (a
# launcher grant). No Knowledge Graph access.
[info]
app_id = "com.notesnook.Notesnook"
tier = "third-party"

[network]
allow_all = true
