# Starting permission profile for Jami (a peer-to-peer messenger and calls). Network is its function; no user files, no
# Knowledge Graph access.
[info]
app_id = "org.jami.Jami"
tier = "third-party"

[network]
allow_all = true
