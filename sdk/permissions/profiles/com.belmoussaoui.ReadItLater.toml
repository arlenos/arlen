# Starting permission profile for Read It Later (an offline article reader).
# It fetches and stores articles over the network by function. It gets network and needs no user files. No Knowledge Graph access.
[info]
app_id = "com.belmoussaoui.ReadItLater"
tier = "third-party"

[network]
allow_all = true
