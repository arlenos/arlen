# Starting permission profile for Heaptrack (a KDE memory-profile visualizer). Opens its data/model files from
# Documents; no network, no Knowledge Graph access.
[info]
app_id = "org.kde.heaptrack"
tier = "third-party"

[filesystem]
documents = true
