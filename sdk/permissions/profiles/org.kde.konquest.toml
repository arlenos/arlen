# Starting permission profile for Konquest (a KDE strategy game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access. Identity only - the maximally fail-closed baseline.
[info]
app_id = "org.kde.konquest"
tier = "third-party"
