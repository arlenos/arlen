# Starting permission profile for Robots (the classic robot-chase game). A self-contained local game; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.gnome.Robots"
tier = "third-party"
