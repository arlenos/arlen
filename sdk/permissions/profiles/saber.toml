# Starting permission profile for a handwriting/notes app. Reads and writes its
# notes under Documents; no network by default (cloud sync needs an explicit
# grant) and no Knowledge Graph. Observe-mode refines it.
[info]
app_id = "saber"
tier = "third-party"

[filesystem]
documents = true
