# Starting permission profile for Warp (magic-wormhole file transfer).
# Sends and receives files over the network by function and saves to Downloads.
# No Knowledge Graph access.
[info]
app_id = "app.drey.Warp"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
