# Starting permission profile for a note-taking app with Nextcloud Notes sync. It
# stores notes as local files (Home) and syncs them to a server (network). No
# Knowledge Graph.
[info]
app_id = "iotas"
tier = "third-party"

[filesystem]
home = true

[network]
allow_all = true
