# Starting permission profile for a cloud note-sync app. Needs network to sync
# notes with its service (any host); notes live in the app, so no document-folder
# access and no Knowledge Graph by default. Observe-mode refines it.
[info]
app_id = "simplenote"
tier = "third-party"

[network]
allow_all = true
