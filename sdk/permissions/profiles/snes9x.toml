# Starting permission profile for Snes9x (a SNES emulator). ROMs and saves live
# in Documents and Downloads; no network and no Knowledge Graph.
#
# ONE ANSWER FOR BOTH PACKAGING IDS (15 Sep): the bare id granted nothing, the
# same gap the other emulator groups had.
[info]
app_id = "snes9x"
tier = "third-party"

[filesystem]
documents = true
downloads = true
