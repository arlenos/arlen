# Starting permission profile for an app-icon generator (Emblem). It exports the
# generated icon, so it gets Pictures; exporting anywhere else goes through the
# file picker, which is a per-file grant. No network.
[info]
app_id = "emblem"
tier = "third-party"

[filesystem]
pictures = true
