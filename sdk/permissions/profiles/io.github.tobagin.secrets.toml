# Starting permission profile for Secrets (a password manager).
# Like a local secrets store it keeps an encrypted vault in a local file and
# gets Documents; it needs no network (sync is opt-in). No Knowledge Graph access.
[info]
app_id = "io.github.tobagin.secrets"
tier = "third-party"

[filesystem]
documents = true
