# Starting permission profile for Extension Manager (browse/install GNOME extensions).
# Browses extensions over the network by function. No Knowledge Graph access.
[info]
app_id = "com.mattjakeman.ExtensionManager"
tier = "third-party"

[network]
allow_all = true
