# Starting permission profile for a streaming/chat client. Needs network to reach
# its service (any host); no local file access and no Knowledge Graph by default.
# Observe-mode refines it.
[info]
app_id = "streamlink-twitch-gui"
tier = "third-party"

[network]
allow_all = true
