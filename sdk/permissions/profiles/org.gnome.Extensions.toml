# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it manages installed shell
# extensions and keeps its settings in its own config dir.
[info]
app_id = "org.gnome.Extensions"
tier = "third-party"
