# Starting permission profile for Halftone (an image dithering tool). It opens
# and saves images through the file portal, so it needs no standing grant: no
# network, no user files and no Knowledge Graph access.
[info]
app_id = "io.github.tfuxu.Halftone"
tier = "third-party"
