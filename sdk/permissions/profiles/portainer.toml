# Starting permission profile for a network client (SSH, scanner, container or
# remote-management tool). Needs network to reach the container host; no local file
# access and no Knowledge Graph by default.
[info]
app_id = "portainer"
tier = "third-party"

[network]
allow_all = true
