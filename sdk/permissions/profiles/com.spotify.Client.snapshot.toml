# Starting permission profile for Spotify (snapshot) (a media streaming client).
# Streaming is its function, so it gets network; downloads save to Downloads.
# No Knowledge Graph access.
[info]
app_id = "com.spotify.Client.snapshot"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
