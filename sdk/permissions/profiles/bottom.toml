# Starting permission profile for a terminal resource monitor. It reads process,
# memory and device state from the kernel (/proc, /sys), which is not a
# permission-profile field, so it needs no file, network or Knowledge Graph grant
# here - the same shape as btop and htop.
[info]
app_id = "bottom"
tier = "third-party"
