# Starting permission profile for a terminal text editor (GNU nano). It edits the
# user's files, so it gets Home; no network of its own and no Knowledge Graph.
[info]
app_id = "nano"
tier = "third-party"

[filesystem]
home = true
