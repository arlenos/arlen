# Starting permission profile for Stimulator (keeps the screen awake).
# It inhibits idle locally. It needs no network and no user files. No Knowledge Graph access.
[info]
app_id = "io.github.sigmasd.stimulator"
tier = "third-party"
