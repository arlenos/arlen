# Starting permission profile for Killbots (a robot-chase game). A self-contained local game; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.kde.killbots"
tier = "third-party"
