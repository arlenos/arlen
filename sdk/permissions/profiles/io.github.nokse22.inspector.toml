# Starting permission profile for Inspector (a system-information viewer).
# It reads system state locally. It needs no network and no user files. No Knowledge Graph access.
[info]
app_id = "io.github.nokse22.inspector"
tier = "third-party"
