# Starting permission profile for Slack (team messaging). Network is its
# function; received files save to Downloads. No graph.
[info]
app_id = "com.slack.Slack"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
