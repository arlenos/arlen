# Starting permission profile for Bavarder (an AI chat client).
# Talks to language-model providers over the network by function. No graph.
[info]
app_id = "io.github.lainsce.Bavarder"
tier = "third-party"

[network]
allow_all = true
