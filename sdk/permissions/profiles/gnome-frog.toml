# Starting permission profile for an OCR text-extraction tool. It reads image files
# to extract text, so it gets Pictures like the other image tools here; the result
# goes to the clipboard through the portal. No network and no Knowledge Graph.
[info]
app_id = "gnome-frog"
tier = "third-party"

[filesystem]
pictures = true
