# Starting permission profile for KBlocks (a falling-blocks game). A self-contained local game; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.kde.kblocks"
tier = "third-party"
