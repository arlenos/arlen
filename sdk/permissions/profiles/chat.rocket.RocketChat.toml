# Starting permission profile for Rocket.Chat (a team-chat desktop client). It
# messages and calls over the network by function and saves received files to
# Downloads. No Knowledge Graph access.
[info]
app_id = "chat.rocket.RocketChat"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
