# Starting permission profile for an image viewer (Loupe, the GNOME image
# viewer). Works with Pictures; no network and no Knowledge Graph.
[info]
app_id = "loupe"
tier = "third-party"

[filesystem]
pictures = true
