# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it reads the system's log files
# (broader targets are granted explicitly).
[info]
app_id = "org.kde.ksystemlog"
tier = "third-party"
