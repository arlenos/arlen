# Starting permission profile for KLines (a KDE puzzle game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access. Identity only - the maximally fail-closed baseline.
[info]
app_id = "org.kde.klines"
tier = "third-party"
