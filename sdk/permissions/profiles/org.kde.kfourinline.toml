# Starting permission profile for KFourInLine (a KDE board game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access. Identity only - the maximally fail-closed baseline.
[info]
app_id = "org.kde.kfourinline"
tier = "third-party"
