# Starting permission profile for KHangMan (a KDE word game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access. Identity only - the maximally fail-closed baseline.
[info]
app_id = "org.kde.khangman"
tier = "third-party"
