# Starting permission profile for File Roller (an archive manager). Archives live in
# Documents and Downloads; no network, no graph.
[info]
app_id = "org.gnome.FileRoller"
tier = "third-party"

[filesystem]
documents = true
downloads = true
