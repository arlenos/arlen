# Starting permission profile for a USB/ISO imaging tool. Reads the disk image
# from Downloads; the write to the USB device is privileged and elevates via
# polkit. No network and no Knowledge Graph.
[info]
app_id = "balena-etcher"
tier = "third-party"

[filesystem]
downloads = true
