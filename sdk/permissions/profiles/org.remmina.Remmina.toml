# Starting permission profile for Remmina (a remote-desktop client).
# Connects to remote hosts (RDP/VNC/SSH) over the network by function. No graph.
[info]
app_id = "org.remmina.Remmina"
tier = "third-party"

[network]
allow_all = true
