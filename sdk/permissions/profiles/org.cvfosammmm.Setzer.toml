# Starting permission profile for Setzer (a LaTeX editor).
# Edits documents; no network by function. No Knowledge Graph access.
[info]
app_id = "org.cvfosammmm.Setzer"
tier = "third-party"

[filesystem]
documents = true
