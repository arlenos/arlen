# Starting permission profile for an office suite. Edits documents under
# Documents; no network by default (cloud storage needs an explicit grant) and no
# Knowledge Graph. Observe-mode refines it.
[info]
app_id = "softmaker-office"
tier = "third-party"

[filesystem]
documents = true
