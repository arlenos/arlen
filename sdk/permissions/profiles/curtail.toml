# Starting permission profile for Curtail (an image compressor). Reads and writes
# images under Pictures; no network and no Knowledge Graph.
[info]
app_id = "curtail"
tier = "third-party"

[filesystem]
pictures = true
