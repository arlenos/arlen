# Starting permission profile for a documents-oriented tool (song sheets, tabs or
# notation). Works with Documents; no network and no Knowledge Graph by default.
# Observe-mode refines it.
[info]
app_id = "chordpro"
tier = "third-party"

[filesystem]
documents = true
