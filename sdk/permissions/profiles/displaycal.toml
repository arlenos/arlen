# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it talks to a colorimeter and its
# generated ICC profiles live in the system colour store (broader paths granted
# explicitly).
[info]
app_id = "displaycal"
tier = "third-party"
