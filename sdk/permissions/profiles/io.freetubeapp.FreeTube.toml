# Starting permission profile for FreeTube (a YouTube client).
# Streams and downloads video over the network by function; saves to Downloads.
# No Knowledge Graph access.
[info]
app_id = "io.freetubeapp.FreeTube"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
