# Starting permission profile for a system/config/utility app that works with the
# user's files. It mounts EncFS-encrypted folders under Home, so it gets Home; no
# network and no Knowledge Graph by default. Observe-mode refines it.
[info]
app_id = "gnome-encfs-manager"
tier = "third-party"

[filesystem]
home = true
