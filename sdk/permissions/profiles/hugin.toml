# Starting permission profile for a panorama photo stitcher. Reads and writes
# images under Pictures; no network and no Knowledge Graph. Observe-mode refines
# it.
[info]
app_id = "hugin"
tier = "third-party"

[filesystem]
pictures = true
