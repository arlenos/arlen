# Starting permission profile for a game mod manager. It downloads mods over the
# network and installs them into game folders under Home, so it gets Home and
# network; no Knowledge Graph. Observe-mode refines it.
[info]
app_id = "r2modman"
tier = "third-party"

[filesystem]
home = true

[network]
allow_all = true
