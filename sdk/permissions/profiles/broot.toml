# Starting permission profile for a terminal tree/file navigator (broot). It browses and manages the user's files,
# so it gets Home; no network of its own and no Knowledge Graph.
[info]
app_id = "broot"
tier = "third-party"

[filesystem]
home = true
