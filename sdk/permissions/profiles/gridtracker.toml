# Starting permission profile for a network client (crypto wallet, remote desktop,
# scanner or web app). Needs network; no local file access and no Knowledge Graph
# by default (a live-map companion fetches map tiles and spots online).
[info]
app_id = "gridtracker"
tier = "third-party"

[network]
allow_all = true
