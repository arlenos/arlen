# Starting permission profile for Secrets (a password manager). Like a local-file
# vault it reads its database under Documents and gets no network (sync is a
# deliberate opt-in). No graph.
[info]
app_id = "org.gnome.World.Secrets"
tier = "third-party"

[filesystem]
documents = true
