# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it draws annotations over the
# screen and its settings live in its own config dir.
[info]
app_id = "gromit-mpx"
tier = "third-party"
