# Starting permission profile for a clipboard manager. It reads and restores what
# the person copies, which is its whole function; no files, no network and no
# Knowledge Graph by default.
[info]
app_id = "xfce4-clipman"
tier = "third-party"

[clipboard]
read = true
