# Starting permission profile for Meteo (a weather app).
# It fetches forecasts over the network by function and needs no user files.
# No Knowledge Graph access.
[info]
app_id = "com.gitlab.bitseater.meteo"
tier = "third-party"

[network]
allow_all = true
