# Starting permission profile for Marker (a Markdown editor). It opens and saves
# documents; no network and no Knowledge Graph access.
[info]
app_id = "it.fabiocolacio.marker"
tier = "third-party"

[filesystem]
documents = true
