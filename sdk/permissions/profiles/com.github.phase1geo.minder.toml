# Starting permission profile for Minder (a mind-mapping app).
# It saves mind-map documents by function, so it gets Documents and no network. No Knowledge Graph access.
[info]
app_id = "com.github.phase1geo.minder"
tier = "third-party"

[filesystem]
documents = true
