# Starting permission profile for Dosage (a medication tracker; the diegopvlk
# GNOME-Circle app, distinct from the diegoivan one). It keeps the schedule in
# its own storage; no network, no user files and no Knowledge Graph access.
[info]
app_id = "io.github.diegopvlk.Dosage"
tier = "third-party"
