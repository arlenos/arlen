# Starting permission profile for KBlackBox (a KDE logic game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access. Identity only - the maximally fail-closed baseline.
[info]
app_id = "org.kde.kblackbox"
tier = "third-party"
