# Starting permission profile for a network client (SSH, scanner, container or
# remote-management tool). Needs network to reach the cluster; no local file access
# and no Knowledge Graph by default (its kubeconfig lives in its own config dir).
[info]
app_id = "k9s"
tier = "third-party"

[network]
allow_all = true
