# Starting permission profile for RustDesk (a remote-desktop client). Connects to
# remote hosts over the network by function; no filesystem grant (file transfer
# is a deliberate opt-in granted on use), no graph.
[info]
app_id = "com.rustdesk.RustDesk"
tier = "third-party"

[network]
allow_all = true
