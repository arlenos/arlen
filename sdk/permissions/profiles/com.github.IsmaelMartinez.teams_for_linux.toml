# Starting permission profile for Teams for Linux (a chat/meetings client).
# Chat and calls over the network by function; saves received files to
# Downloads. No Knowledge Graph access.
[info]
app_id = "com.github.IsmaelMartinez.teams_for_linux"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
