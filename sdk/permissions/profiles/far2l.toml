# Starting permission profile for a file/archive manager. It works wherever the
# person keeps their files, so anything narrower would leave folders it cannot
# open; no network and no Knowledge Graph by default.
[info]
app_id = "far2l"
tier = "third-party"

[filesystem]
home = true
