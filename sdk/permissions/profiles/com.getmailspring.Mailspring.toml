# Starting permission profile for Mailspring (an email client).
# Sends and receives mail over the network by function; saves attachments to
# Downloads. No Knowledge Graph access.
[info]
app_id = "com.getmailspring.Mailspring"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
