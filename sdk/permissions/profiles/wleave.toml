# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it presents a session logout menu
# and runs the configured session commands.
[info]
app_id = "wleave"
tier = "third-party"
