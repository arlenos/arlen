# Starting permission profile for HexChat (an IRC client).
# Chat over the network by function; saves DCC transfers to Downloads. No graph.
[info]
app_id = "org.hexchat.HexChat"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
