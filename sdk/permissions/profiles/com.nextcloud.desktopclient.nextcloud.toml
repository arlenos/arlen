# Starting permission profile for the Nextcloud desktop sync client. Syncing is
# its function, so it gets network; the synced folder lives under Documents. No
# graph.
[info]
app_id = "com.nextcloud.desktopclient.nextcloud"
tier = "third-party"

[filesystem]
documents = true

[network]
allow_all = true
