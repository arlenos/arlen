# Starting permission profile for an office/document app. Edits or reads
# documents; no network (offline by function), no graph.
[info]
app_id = "okular"
tier = "third-party"

[filesystem]
documents = true
