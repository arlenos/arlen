# Starting permission profile for Jellyfin Media Player (a media streaming client).
# Streaming is its function, so it gets network; downloads save to Downloads.
# No Knowledge Graph access.
[info]
app_id = "io.github.iwalton3.jellyfin-media-player"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
