# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it adjusts display settings and
# keeps its own config in its own dir.
[info]
app_id = "gammy"
tier = "third-party"
