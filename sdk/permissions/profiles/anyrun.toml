# Starting permission profile for anyrun (an application launcher). Lists and starts
# installed apps; no file access, no network and no Knowledge Graph by default.
[info]
app_id = "anyrun"
tier = "third-party"
