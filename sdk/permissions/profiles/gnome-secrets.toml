# Starting permission profile for a password manager. Opens its database file
# under Documents; no network by default (sync needs an explicit grant) and no
# Knowledge Graph. Observe-mode refines it.
[info]
app_id = "gnome-secrets"
tier = "third-party"

[filesystem]
documents = true
