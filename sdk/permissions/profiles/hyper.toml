# Starting permission profile for a terminal emulator. It runs the user's shell
# and tools over their files, so it gets Home; no network of its own (config sync
# and the tools it launches carry their own grants). No Knowledge Graph.
[info]
app_id = "hyper"
tier = "third-party"

[filesystem]
home = true
