# Starting permission profile for Upscayl (an AI image upscaler). Reads and writes
# images under Pictures using local models; no network and no Knowledge Graph.
[info]
app_id = "upscayl"
tier = "third-party"

[filesystem]
pictures = true
