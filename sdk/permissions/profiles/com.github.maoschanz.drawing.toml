# Starting permission profile for Drawing (a basic image editor).
# Reads and writes images in Pictures; no network, no graph.
[info]
app_id = "com.github.maoschanz.drawing"
tier = "third-party"

[filesystem]
pictures = true
