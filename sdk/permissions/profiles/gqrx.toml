# Starting permission profile for the Gqrx SDR receiver. Talks to SDR hardware
# and keeps its settings in its own config dir, so it needs no document folders,
# no network and no Knowledge Graph. Recording baseband or audio to a chosen
# folder needs an explicit grant. Observe-mode refines it.
[info]
app_id = "gqrx"
tier = "third-party"
