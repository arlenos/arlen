# Starting permission profile for a hackable terminal file explorer (xplr). It browses and manages the user's files,
# so it gets Home; no network of its own and no Knowledge Graph.
[info]
app_id = "xplr"
tier = "third-party"

[filesystem]
home = true
