# Starting permission profile for KCachegrind (a KDE profile-data visualizer). Opens its data/model files from
# Documents; no network, no Knowledge Graph access.
[info]
app_id = "org.kde.kcachegrind"
tier = "third-party"

[filesystem]
documents = true
