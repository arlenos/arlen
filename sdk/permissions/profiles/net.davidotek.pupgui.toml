# Starting permission profile for ProtonUp-Qt (a Proton/Wine compatibility-tool installer).
# It downloads compatibility-tool builds over the network by function. It gets network and needs no user files. No Knowledge Graph access.
[info]
app_id = "net.davidotek.pupgui"
tier = "third-party"

[network]
allow_all = true
