# Starting permission profile for Folio (a markdown notes app).
# Notes are kept in the app's own storage. It needs no network and no user files. No Knowledge Graph access.
[info]
app_id = "com.toolstack.Folio"
tier = "third-party"
