# Starting permission profile for Evolution (email and PIM). Network (IMAP/SMTP/
# CalDAV) is its function; attachments save to Downloads. No graph.
[info]
app_id = "org.gnome.Evolution"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
