# Starting permission profile for Authenticator (a TOTP two-factor code app). Keeps
# its secrets in its own config dir; no file access, no network and no Knowledge
# Graph by default.
[info]
app_id = "authenticator"
tier = "third-party"
