# Starting permission profile for Paper Plane (a Telegram client).
# Chat and calls over the network by function; saves received files to
# Downloads. No Knowledge Graph access.
[info]
app_id = "app.drey.PaperPlane"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
