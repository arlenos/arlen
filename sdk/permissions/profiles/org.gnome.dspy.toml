# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it inspects the session bus and
# keeps its settings in its own config dir.
[info]
app_id = "org.gnome.dspy"
tier = "third-party"
