# Starting permission profile for a yt-dlp front end. It downloads media over the
# network by function; it gets network and needs no user files (the download folder
# is granted explicitly). No Knowledge Graph access.
[info]
app_id = "org.nickvision.tubeconverter"
tier = "third-party"

[network]
allow_all = true
