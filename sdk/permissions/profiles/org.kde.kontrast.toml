# Starting permission profile for Kontrast (a colour-contrast checker). A self-contained local tool; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.kde.kontrast"
tier = "third-party"
