# Starting permission profile for Resources (a system-resource monitor). Reads
# system statistics only; no file access, no network and no Knowledge Graph.
[info]
app_id = "resources"
tier = "third-party"
