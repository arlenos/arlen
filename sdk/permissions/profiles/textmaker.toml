# Starting permission profile for textmaker (an office application). Opens and saves
# documents under Documents; no network and no Knowledge Graph by default.
[info]
app_id = "textmaker"
tier = "third-party"

[filesystem]
documents = true
