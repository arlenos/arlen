# Starting permission profile for walker (an application launcher). Lists and starts
# installed apps; no file access, no network and no Knowledge Graph by default.
[info]
app_id = "walker"
tier = "third-party"
