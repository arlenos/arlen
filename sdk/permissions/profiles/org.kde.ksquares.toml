# Starting permission profile for KSquares (the dots-and-boxes game). A self-contained local game; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.kde.ksquares"
tier = "third-party"
