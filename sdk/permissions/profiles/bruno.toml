# Starting permission profile for a cloud-music/API-client app. Needs network to
# reach its service; no local file access and no Knowledge Graph by default.
[info]
app_id = "bruno"
tier = "third-party"

[network]
allow_all = true
