# Starting permission profile for Reversi (the disc-flipping strategy game). A self-contained local game; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.gnome.Reversi"
tier = "third-party"
