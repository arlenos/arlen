# Starting permission profile for Blinken (a KDE memory game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access. Identity only - the maximally fail-closed baseline.
[info]
app_id = "org.kde.blinken"
tier = "third-party"
