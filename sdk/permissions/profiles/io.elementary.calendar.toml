# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; its data lives in its own config
# dir or in the desktop's shared services (calendar/tasks via online-accounts).
[info]
app_id = "io.elementary.calendar"
tier = "third-party"
