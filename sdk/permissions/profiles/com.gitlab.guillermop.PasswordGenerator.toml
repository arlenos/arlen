# Starting permission profile for Password Generator.
# It generates passwords locally. It needs no network and no user files. No Knowledge Graph access.
[info]
app_id = "com.gitlab.guillermop.PasswordGenerator"
tier = "third-party"
