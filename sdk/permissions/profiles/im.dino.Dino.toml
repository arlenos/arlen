# Starting permission profile for Dino (an XMPP chat client).
# Chat over the network by function; saves received files to Downloads. No graph.
[info]
app_id = "im.dino.Dino"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
