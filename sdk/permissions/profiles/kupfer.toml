# Starting permission profile for the Kupfer launcher. Reads the installed-app
# index and keeps its settings in its own config dir, so it needs no document
# folders, no network and no Knowledge Graph by default. Observe-mode refines it.
[info]
app_id = "kupfer"
tier = "third-party"
