# Starting permission profile for Picmi (a nonogram / picross puzzle). A self-contained local game; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.kde.picmi"
tier = "third-party"
