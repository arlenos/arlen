# Starting permission profile for an image app. Works with Pictures; no network
# and no Knowledge Graph by default.
[info]
app_id = "photofilmstrip"
tier = "third-party"

[filesystem]
pictures = true
