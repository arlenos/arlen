# Starting permission profile for DBeaver (a database client).
# Connects to database servers over the network by function. No graph.
[info]
app_id = "io.dbeaver.DBeaverCommunity"
tier = "third-party"

[network]
allow_all = true
