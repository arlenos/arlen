# Starting permission profile for Letterpress (converts an image to ASCII art).
# It reads a picture and writes the text result; pictures, no network, no graph.
[info]
app_id = "io.gitlab.gregorni.Letterpress"
tier = "third-party"

[filesystem]
pictures = true
