# Starting permission profile for a video/RSS frontend. Needs network to fetch
# content; no local file access and no Knowledge Graph by default.
[info]
app_id = "freetube"
tier = "third-party"

[network]
allow_all = true
