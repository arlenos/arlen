# Starting permission profile for Glade (a GTK interface designer).
# Edits interface files in Documents; no network by function. No graph.
[info]
app_id = "org.gnome.Glade"
tier = "third-party"

[filesystem]
documents = true
