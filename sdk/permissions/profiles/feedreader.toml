# Starting permission profile for an RSS/news reader. Needs network to fetch
# feeds; no local file access and no Knowledge Graph by default.
[info]
app_id = "feedreader"
tier = "third-party"

[network]
allow_all = true
