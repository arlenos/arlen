# Starting permission profile for Buffer (a quick-notes scratchpad).
# Notes are kept in the app's own storage. It needs no network and no user files. No Knowledge Graph access.
[info]
app_id = "org.gnome.gitlab.cheywood.Buffer"
tier = "third-party"
