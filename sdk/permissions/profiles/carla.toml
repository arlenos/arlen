# Starting permission profile for Carla (an audio plugin host). Works with Music
# (audio and plugin projects); no network and no Knowledge Graph.
[info]
app_id = "carla"
tier = "third-party"

[filesystem]
music = true
