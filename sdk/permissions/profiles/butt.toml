# Starting permission profile for a network client (audio broadcaster). Streams
# audio to a broadcast server over the network by function, so it gets network; no
# local file access and no Knowledge Graph by default.
[info]
app_id = "butt"
tier = "third-party"

[network]
allow_all = true
