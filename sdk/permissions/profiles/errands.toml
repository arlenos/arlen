# Starting permission profile for Errands (a to-do app). Keeps tasks in its own
# data dir; no file access, no network and no Knowledge Graph by default (sync is
# granted explicitly).
[info]
app_id = "errands"
tier = "third-party"
