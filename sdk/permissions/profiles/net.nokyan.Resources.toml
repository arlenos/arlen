# Starting permission profile for Resources (a system resource monitor).
# It reads system state (CPU/memory/disk/processes) by function and needs no
# network, no user files and no Knowledge Graph access.
[info]
app_id = "net.nokyan.Resources"
tier = "third-party"
