# Starting permission profile for an internet-radio or IPTV player. Streams
# channels over the network; no local file access and no Knowledge Graph by
# default. Observe-mode refines it.
[info]
app_id = "hypnotix"
tier = "third-party"

[network]
allow_all = true
