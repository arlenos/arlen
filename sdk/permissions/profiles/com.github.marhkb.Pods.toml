# Starting permission profile for Pods (a Podman desktop client). Talks to the
# Podman socket and container registries over the network by function; it manages
# containers and images, not the user's files, so no filesystem or graph access.
[info]
app_id = "com.github.marhkb.Pods"
tier = "third-party"

[network]
allow_all = true
