# Starting permission profile for a firmware/hardware-info utility. No file access,
# no network and no Knowledge Graph; it reads hardware info over the system bus and
# /sys and elevates via fwupd/polkit for firmware operations.
[info]
app_id = "hardinfo"
tier = "third-party"
