# Starting permission profile for WSJT-X (weak-signal ham modes like FT8). Writes
# its logs to its own data dir, so it needs no document folders and no Knowledge
# Graph. Uploading spots to PSK Reporter needs an explicit network grant.
# Observe-mode refines it.
[info]
app_id = "wsjtx"
tier = "third-party"
