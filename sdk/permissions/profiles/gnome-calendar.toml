# Starting permission profile for a self-contained GNOME app (calendar or
# recipes). No file access, no network and no Knowledge Graph by default;
# calendars sync via online-accounts and data lives in its own dir.
[info]
app_id = "gnome-calendar"
tier = "third-party"
