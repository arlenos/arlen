# Starting permission profile for Falkon (a KDE web browser). A browser's function is the open web, so
# it gets broad network; it saves to Downloads. No Knowledge Graph access.
[info]
app_id = "org.kde.falkon"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
