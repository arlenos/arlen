# Starting permission profile for Embellish (a Nerd Fonts installer).
# It downloads and installs fonts over the network by function. It gets network and needs no user files. No Knowledge Graph access.
[info]
app_id = "io.github.getnf.embellish"
tier = "third-party"

[network]
allow_all = true
