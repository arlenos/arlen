# Starting permission profile for Kollision (a ball-dodging game). A self-contained local game; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.kde.kollision"
tier = "third-party"
