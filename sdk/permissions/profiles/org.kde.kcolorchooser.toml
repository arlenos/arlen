# Starting permission profile for KColorChooser (a screen colour picker). It copies
# the picked colour code to the clipboard; no network, no user files, no graph.
[info]
app_id = "org.kde.kcolorchooser"
tier = "third-party"

[clipboard]
write = true
