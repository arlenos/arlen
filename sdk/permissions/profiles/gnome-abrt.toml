# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it reviews local crash reports
# (uploading a report is granted explicitly).
[info]
app_id = "gnome-abrt"
tier = "third-party"
