# Starting permission profile for KTeaTime (a tea-steeping timer). A self-contained local tool; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "org.kde.kteatime"
tier = "third-party"
