# Starting permission profile for a network client (Gemini/gopher browser). Fetches
# pages over the network by function, so it gets network; no local file access and
# no Knowledge Graph by default (downloads are saved through an explicit grant).
[info]
app_id = "kristall"
tier = "third-party"

[network]
allow_all = true
