# Starting permission profile for a minimal image viewer (sxiv, the simple X
# image viewer, nsxiv's predecessor). Views images from Pictures; no network and
# no Knowledge Graph.
[info]
app_id = "sxiv"
tier = "third-party"

[filesystem]
pictures = true
