# Starting permission profile for xemu (an original Xbox emulator). Games, BIOS
# images and saves live in Documents and Downloads; no network and no Knowledge
# Graph.
#
# ONE ANSWER FOR BOTH PACKAGING IDS (15 Sep): the bare id granted nothing, the
# same gap as every other emulator group in this catalogue.
[info]
app_id = "xemu"
tier = "third-party"

[filesystem]
documents = true
downloads = true
