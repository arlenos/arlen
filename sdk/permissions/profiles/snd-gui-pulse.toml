# Starting permission profile for an audio app. Works with the Music folder; no
# network and no Knowledge Graph by default.
[info]
app_id = "snd-gui-pulse"
tier = "third-party"

[filesystem]
music = true
