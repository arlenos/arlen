# Starting permission profile for a planetarium app. No file access, no network
# and no Knowledge Graph by default; its star catalogues live in its own data dir
# (extra catalogue downloads are refined later).
[info]
app_id = "celestia"
tier = "third-party"
