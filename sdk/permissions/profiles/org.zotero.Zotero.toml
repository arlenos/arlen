# Starting permission profile for Zotero (reference manager). Network is its
# function (library sync + metadata/citation lookup); attached PDFs and exports
# live in Documents. No graph.
[info]
app_id = "org.zotero.Zotero"
tier = "third-party"

[filesystem]
documents = true

[network]
allow_all = true
