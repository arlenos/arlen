# Starting permission profile for Celeste (a cloud file-sync client). Syncing is
# its function, so it gets network; the synced folder lives under Documents. No
# Knowledge Graph access.
[info]
app_id = "com.hunterwittenborn.Celeste"
tier = "third-party"

[filesystem]
documents = true

[network]
allow_all = true
