# Starting permission profile for Klavaro (a touch-typing tutor). A self-contained app: no file access, no
# network and no Knowledge Graph - its data lives in its own app directory.
[info]
app_id = "klavaro"
tier = "third-party"
