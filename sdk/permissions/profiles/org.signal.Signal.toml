# Starting permission profile for Signal Desktop (messaging). Network is its
# function; received attachments save to Downloads. No graph.
[info]
app_id = "org.signal.Signal"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
