# Starting permission profile for a terminal emulator. It runs the user's shell
# and tools over their files, so it gets Home; no network by default (its AI
# features are refined later). No Knowledge Graph.
[info]
app_id = "warp-terminal"
tier = "third-party"

[filesystem]
home = true
