# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it presents an application
# overview and keeps its settings in its own config dir.
[info]
app_id = "xfdashboard"
tier = "third-party"
