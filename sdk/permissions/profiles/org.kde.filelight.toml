# Starting permission profile for Filelight (a disk-usage analyzer). Scans the user's
# directories (read), so it gets Home; no network, no graph.
[info]
app_id = "org.kde.filelight"
tier = "third-party"

[filesystem]
home = true
