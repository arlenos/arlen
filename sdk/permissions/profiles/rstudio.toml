# Starting permission profile for a science/math tool. Works with Documents; no
# network and no Knowledge Graph by default (package fetches refined later).
[info]
app_id = "rstudio"
tier = "third-party"

[filesystem]
documents = true
