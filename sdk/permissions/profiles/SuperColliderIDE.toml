# Starting permission profile for the SuperCollider IDE (audio programming). Projects
# and sample libraries live across Home, and Quark package installs fetch over the
# network. No Knowledge Graph access.
[info]
app_id = "SuperColliderIDE"
tier = "third-party"

[filesystem]
home = true

[network]
allow_all = true
