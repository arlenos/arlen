# Starting permission profile for an API client. Issues HTTP requests over the
# network by function. No Knowledge Graph access.
[info]
app_id = "hoppscotch"
tier = "third-party"

[network]
allow_all = true
