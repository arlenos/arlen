# Starting permission profile for a download/podcast manager. Needs network to
# fetch; saves to Downloads. No Knowledge Graph access.
[info]
app_id = "jdownloader"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
