# Starting permission profile for OpenShot (a video editor).
# Reads source media and writes renders; no network by function. No graph.
[info]
app_id = "org.openshot.OpenShot"
tier = "third-party"

[filesystem]
videos = true
music = true
pictures = true
