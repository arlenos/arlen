# Starting permission profile for Snapshot (the GNOME camera app). Captured
# photos and videos save to Pictures; no network, no graph.
[info]
app_id = "org.gnome.Snapshot"
tier = "third-party"

[filesystem]
pictures = true
