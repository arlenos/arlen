# Starting permission profile for Standard Notes (encrypted notes with sync).
# Syncs notes over the network by function; notes live in its own config dir (a
# launcher grant). No Knowledge Graph access.
[info]
app_id = "org.standardnotes.standardnotes"
tier = "third-party"

[network]
allow_all = true
