# Starting permission profile for a Gemini-protocol browser. Needs network (any
# host) and saves files to Downloads; no other file access and no Knowledge Graph
# by default. Observe-mode refines it.
[info]
app_id = "lagrange"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
