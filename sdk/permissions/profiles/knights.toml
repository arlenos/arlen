# Starting permission profile for Knights (the KDE chess board). Opens and saves its documents from
# Documents; no network and no Knowledge Graph access.
[info]
app_id = "knights"
tier = "third-party"

[filesystem]
documents = true
