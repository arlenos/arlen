# Starting permission profile for a network client (screen casting to network
# displays). Streams the screen to Miracast/network receivers by function, so it
# gets network; no local file access and no Knowledge Graph by default.
[info]
app_id = "org.gnome.NetworkDisplays"
tier = "third-party"

[network]
allow_all = true
