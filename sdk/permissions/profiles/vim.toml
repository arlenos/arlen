# Starting permission profile for a terminal code editor. It edits the user's files
# across their tree and spawns tooling that carries its own grants, so it gets
# Home; no network of its own. No Knowledge Graph.
[info]
app_id = "vim"
tier = "third-party"

[filesystem]
home = true
