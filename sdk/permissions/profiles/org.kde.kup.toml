# Starting permission profile for a sync/backup tool. It synchronises or backs up
# the user's files, so it gets Home and network; no Knowledge Graph access.
# Observe-mode refines it.
[info]
app_id = "org.kde.kup"
tier = "third-party"

[filesystem]
home = true

[network]
allow_all = true
