# Starting permission profile for Ear Tag (a music-metadata editor).
# It edits the tags of audio files by function, so it gets Music. It gets Music and no network. No Knowledge Graph access.
[info]
app_id = "io.github.knuxify.Ear-Tag"
tier = "third-party"

[filesystem]
music = true
