# Starting permission profile for KAddressBook (a contacts manager). Local address books in Documents; CardDAV sync is an opt-in network grant.
[info]
app_id = "org.kde.kaddressbook"
tier = "third-party"

[filesystem]
documents = true
