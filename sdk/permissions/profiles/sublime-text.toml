# Starting permission profile for a text/code editor. Edits documents; no network
# and no Knowledge Graph by default (projects outside Documents, or package
# downloads, need an explicit grant). Observe-mode refines it.
[info]
app_id = "sublime-text"
tier = "third-party"

[filesystem]
documents = true
