# Starting permission profile for WhatIP (a network information tool).
# It queries the host's and the public IP/network details over the network by
# function, needing no user files. No Knowledge Graph access.
[info]
app_id = "org.gabmus.whatip"
tier = "third-party"

[network]
allow_all = true
