# Starting permission profile for KWeather. Fetches forecasts over the network
# (its function); no filesystem, no graph.
[info]
app_id = "org.kde.kweather"
tier = "third-party"

[network]
allow_all = true
