# Starting permission profile for a LaTeX editor. Edits documents and invokes the
# TeX toolchain on them; no network and no Knowledge Graph by default.
[info]
app_id = "texstudio"
tier = "third-party"

[filesystem]
documents = true
