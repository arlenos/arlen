# Starting permission profile for Live Captions (on-device captions of system audio). A self-contained local tool; no network,
# no user files, no Knowledge Graph access.
[info]
app_id = "net.sapples.LiveCaptions"
tier = "third-party"
