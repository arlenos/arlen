# Starting permission profile for Parley (the KDE vocabulary trainer). Opens and saves its documents from
# Documents; no network and no Knowledge Graph access.
[info]
app_id = "parley"
tier = "third-party"

[filesystem]
documents = true
