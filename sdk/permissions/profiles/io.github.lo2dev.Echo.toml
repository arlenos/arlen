# Starting permission profile for Echo (a hash generator).
# It computes hashes of entered text locally. It needs no network and no user files. No Knowledge Graph access.
[info]
app_id = "io.github.lo2dev.Echo"
tier = "third-party"
