# Starting permission profile for an XMPP/Jabber console client (Profanity). It connects to chat servers over the
# network; no local file access and no Knowledge Graph by default.
[info]
app_id = "profanity"
tier = "third-party"

[network]
allow_all = true
