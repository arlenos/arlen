# Starting permission profile for an HDR photo tool. Reads and writes images
# under Pictures; no network and no Knowledge Graph. Observe-mode refines it.
[info]
app_id = "luminance-hdr"
tier = "third-party"

[filesystem]
pictures = true
