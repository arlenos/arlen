# Starting permission profile for Mnemosyne (a flashcard/spaced-repetition app).
# Imports and exports card decks under Documents; no network by default (deck sync
# needs an explicit grant) and no Knowledge Graph. Observe-mode refines it.
[info]
app_id = "mnemosyne"
tier = "third-party"

[filesystem]
documents = true
