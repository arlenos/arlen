# Starting permission profile for RawTherapee (RAW photo editor). Reads and
# writes images in Pictures; no network, no graph.
[info]
app_id = "com.rawtherapee.RawTherapee"
tier = "third-party"

[filesystem]
pictures = true
