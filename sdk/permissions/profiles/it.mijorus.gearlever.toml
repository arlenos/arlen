# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it integrates AppImages the user
# picks into its own app dir (broader paths are granted explicitly).
[info]
app_id = "it.mijorus.gearlever"
tier = "third-party"
