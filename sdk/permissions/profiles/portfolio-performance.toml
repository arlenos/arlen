# Starting permission profile for a personal-finance app. Stores its ledger under
# Documents; no network by default (online quotes need an explicit grant) and no
# Knowledge Graph. Observe-mode refines it.
[info]
app_id = "portfolio-performance"
tier = "third-party"

[filesystem]
documents = true
