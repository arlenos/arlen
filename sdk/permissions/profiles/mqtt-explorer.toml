# Starting permission profile for a network client (MQTT/IoT). Connects to message
# brokers over the network by function, so it gets network; no local file access
# and no Knowledge Graph by default.
[info]
app_id = "mqtt-explorer"
tier = "third-party"

[network]
allow_all = true
