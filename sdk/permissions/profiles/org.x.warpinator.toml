# Starting permission profile for Warpinator (local-network file transfer).
# It sends and receives files across the local network by function; received files save to Downloads. It gets network and Downloads. No Knowledge Graph access.
[info]
app_id = "org.x.warpinator"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
