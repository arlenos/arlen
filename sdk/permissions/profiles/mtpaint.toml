# Starting permission profile for a pixel/image editor. Reads and writes images
# under Pictures; no network and no Knowledge Graph. Observe-mode refines it.
[info]
app_id = "mtpaint"
tier = "third-party"

[filesystem]
pictures = true
