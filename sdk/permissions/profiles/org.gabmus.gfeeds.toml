# Starting permission profile for Feeds (an RSS/Atom reader).
# Fetches feeds over the network by function. No Knowledge Graph access.
[info]
app_id = "org.gabmus.gfeeds"
tier = "third-party"

[network]
allow_all = true
