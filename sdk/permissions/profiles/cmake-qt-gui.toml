# Starting permission profile for a documents/notes app. Works with Documents; no
# network and no Knowledge Graph by default (cloud sync is refined later).
[info]
app_id = "cmake-qt-gui"
tier = "third-party"

[filesystem]
documents = true
