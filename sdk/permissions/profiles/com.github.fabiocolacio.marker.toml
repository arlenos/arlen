# Starting permission profile for Marker (a markdown editor).
# It edits markdown documents the user opens by function, so it gets Documents and no network. No Knowledge Graph access.
[info]
app_id = "com.github.fabiocolacio.marker"
tier = "third-party"

[filesystem]
documents = true
