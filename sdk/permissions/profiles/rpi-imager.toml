# Starting permission profile for an SD-card imaging tool. Writing a raw block
# device is privileged and elevates via polkit, never through this profile, and
# the image it flashes is chosen through the file picker. So no standing file
# access, no network and no Knowledge Graph.
[info]
app_id = "rpi-imager"
tier = "third-party"
