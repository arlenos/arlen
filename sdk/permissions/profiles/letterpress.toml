# Starting permission profile for an image-to-ASCII-art converter (Letterpress). It
# opens an image and saves/copies the text art, so it gets home filesystem access;
# it uses no network.
[info]
app_id = "letterpress"
tier = "third-party"

[filesystem]
home = true
