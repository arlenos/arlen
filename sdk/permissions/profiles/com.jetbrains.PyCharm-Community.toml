# Starting permission profile for PyCharm Community (a Python IDE).
# Fetches packages over the network; projects live in Documents. No graph.
[info]
app_id = "com.jetbrains.PyCharm-Community"
tier = "third-party"

[filesystem]
documents = true

[network]
allow_all = true
