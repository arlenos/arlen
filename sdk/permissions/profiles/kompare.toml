# Starting permission profile for kompare (a diff viewer). Opens and saves files under
# Documents; no network and no Knowledge Graph by default. Observe-mode refines it.
[info]
app_id = "kompare"
tier = "third-party"

[filesystem]
documents = true
