# Starting permission profile for Snapshot (a camera app). Saves photos to Pictures
# and videos to Videos; no network and no Knowledge Graph.
[info]
app_id = "snapshot"
tier = "third-party"

[filesystem]
pictures = true
videos = true
