# Starting permission profile for a terminal music player (musikcube). It plays the user's music library, so it
# gets Music; no network of its own and no Knowledge Graph.
[info]
app_id = "musikcube"
tier = "third-party"

[filesystem]
music = true
