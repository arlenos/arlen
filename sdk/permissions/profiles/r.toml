# Starting permission profile for the R console. A statistics workspace: scripts and
# data sets live anywhere under Home, and CRAN package installs need the network.
# No Knowledge Graph access.
[info]
app_id = "r"
tier = "third-party"

[filesystem]
home = true

[network]
allow_all = true
