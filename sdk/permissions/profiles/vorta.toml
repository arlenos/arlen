# Starting permission profile for Vorta (a BorgBackup GUI). Backs up files under
# home to local or remote repositories over the network; no Knowledge Graph.
[info]
app_id = "vorta"
tier = "third-party"

[filesystem]
home = true

[network]
allow_all = true
