# Starting permission profile for a system/config/utility app. No file access,
# no network and no Knowledge Graph by default; it manages the GnuPG keyring in
# its own store (importing key files is granted explicitly).
[info]
app_id = "org.kde.kleopatra"
tier = "third-party"
