# Starting permission profile for GOverlay (a MangoHud/vkBasalt overlay config GUI).
# It edits the overlay config in its own config directory; no broad filesystem
# grant, no network and no Knowledge Graph access.
[info]
app_id = "io.github.benjamimgois.goverlay"
tier = "third-party"
