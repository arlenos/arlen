# Starting permission profile for a file-sync client. It synchronises the user's
# files, so it gets Home and network; no Knowledge Graph access.
[info]
app_id = "megasync"
tier = "third-party"

[filesystem]
home = true

[network]
allow_all = true
