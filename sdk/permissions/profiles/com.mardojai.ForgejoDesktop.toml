# Starting permission profile for Forgejo Desktop (a git-forge client).
# It connects to Forgejo/Gitea servers over the network by function. It gets network and needs no user files. No Knowledge Graph access.
[info]
app_id = "com.mardojai.ForgejoDesktop"
tier = "third-party"

[network]
allow_all = true
