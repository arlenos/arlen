# Starting permission profile for a notes/PKM/finance/project/task tool. Works
# with Documents (notes, exports); no network by default (account sync needs an
# explicit grant) and no Knowledge Graph.
[info]
app_id = "nixnote2"
tier = "third-party"

[filesystem]
documents = true
