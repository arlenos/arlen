# Starting permission profile for a file manager. Browsing the user's files is
# its function, so it gets Home; no network and no Knowledge Graph.
[info]
app_id = "com.system76.CosmicFiles"
tier = "third-party"

[filesystem]
home = true
