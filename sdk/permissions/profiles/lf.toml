# Starting permission profile for a terminal file manager (lf). It browses and manages the user's files,
# so it gets Home; no network of its own and no Knowledge Graph.
[info]
app_id = "lf"
tier = "third-party"

[filesystem]
home = true
