# Starting permission profile for WebCord (a Discord client).
# Chat and calls over the network by function; saves received files to
# Downloads. No Knowledge Graph access.
[info]
app_id = "io.github.spacingbat3.webcord"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
