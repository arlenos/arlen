# Starting permission profile for Video Downloader (a yt-dlp GUI).
# Fetches media over the network by function; saves it to Downloads. No graph.
[info]
app_id = "io.github.JaGoLi.ytdl_gui"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
