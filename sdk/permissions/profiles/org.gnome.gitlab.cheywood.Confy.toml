# Starting permission profile for Confy (a conference-schedule viewer).
# It fetches event schedules over the network by function. It gets network and needs no user files. No Knowledge Graph access.
[info]
app_id = "org.gnome.gitlab.cheywood.Confy"
tier = "third-party"

[network]
allow_all = true
