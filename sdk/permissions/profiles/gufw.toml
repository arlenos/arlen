# Starting permission profile for a firewall-configuration utility. No file
# access, no network and no Knowledge Graph by default; its privileged operations
# elevate via polkit. Observe-mode refines it.
[info]
app_id = "gufw"
tier = "third-party"
