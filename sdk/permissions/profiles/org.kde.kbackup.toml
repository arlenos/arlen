# Starting permission profile for KBackup (a backup tool). Reads the user's files
# to archive them, so it gets Home; local backup, no network, no graph.
[info]
app_id = "org.kde.kbackup"
tier = "third-party"

[filesystem]
home = true
