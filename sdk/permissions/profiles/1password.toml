# Starting permission profile for a network client (password manager or cloud
# storage). Its function is online sync, so it gets network; it needs no media
# dirs and no Knowledge Graph access.
[info]
app_id = "1password"
tier = "third-party"

[network]
allow_all = true
