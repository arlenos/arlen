# Starting permission profile for KSame (a KDE single-player tile game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access.
[info]
app_id = "org.kde.ksame"
tier = "third-party"
