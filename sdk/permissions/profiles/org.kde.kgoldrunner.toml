# Starting permission profile for KGoldrunner (a KDE arcade game). Plays locally; needs no filesystem,
# network, or Knowledge Graph access. Identity only - the maximally fail-closed baseline.
[info]
app_id = "org.kde.kgoldrunner"
tier = "third-party"
