# Starting permission profile for Sequeler (a SQL database client).
# Connects to database servers over the network by function. No graph.
[info]
app_id = "com.github.alecaddd.sequeler"
tier = "third-party"

[network]
allow_all = true
