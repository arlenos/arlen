# Starting permission profile for ScummVM (an adventure-game engine). The game opens any data files the user
# provides through the file portal, so it needs no standing grant; no network, no
# Knowledge Graph access.
[info]
app_id = "org.scummvm.ScummVM"
tier = "third-party"
