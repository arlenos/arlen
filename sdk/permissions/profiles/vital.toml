# Starting permission profile for an audio synth / server utility. Produces or
# routes audio via the audio system; its presets live in its own config dir, so
# no document-folder access, no network and no Knowledge Graph by default.
# Observe-mode refines it.
[info]
app_id = "vital"
tier = "third-party"
