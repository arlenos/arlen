# Starting permission profile for a network utility (weather or online
# dictionary). Needs network to fetch; no local file access and no Knowledge
# Graph.
[info]
app_id = "gnome-weather"
tier = "third-party"

[network]
allow_all = true
