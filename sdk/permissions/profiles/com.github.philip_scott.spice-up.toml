# Starting permission profile for Spice-Up (a presentation editor). It opens and
# saves presentation files through the file portal, so it needs no standing
# grant: no network, no user files and no Knowledge Graph access.
[info]
app_id = "com.github.philip_scott.spice-up"
tier = "third-party"
