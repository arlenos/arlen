# Starting permission profile for Parsec (remote desktop and game streaming).
# It streams a remote desktop over the network by function. It gets network and needs no user files. No Knowledge Graph access.
[info]
app_id = "org.parsecgaming.parsec"
tier = "third-party"

[network]
allow_all = true
