# Starting permission profile for Rocket.Chat (a chat client).
# Messaging over the network by function; saves received files to Downloads.
# No Knowledge Graph access.
[info]
app_id = "chat.rocket.RocketChatDesktop"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
