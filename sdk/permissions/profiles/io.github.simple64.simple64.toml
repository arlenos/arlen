# Starting permission profile for simple64 (a Nintendo 64 emulator). The game opens any data files the user
# provides through the file portal, so it needs no standing grant; no network, no
# Knowledge Graph access.
[info]
app_id = "io.github.simple64.simple64"
tier = "third-party"
