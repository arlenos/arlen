# Starting permission profile for a code editor/IDE. Edits LaTeX projects under
# Documents; no network and no Knowledge Graph by default (projects elsewhere need
# an explicit grant). Observe-mode refines it.
[info]
app_id = "gnome-latex"
tier = "third-party"

[filesystem]
documents = true
