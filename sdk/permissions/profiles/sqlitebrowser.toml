# Starting permission profile for a database browser. It opens database files the
# user points it at, which live with their documents; no network and no Knowledge
# Graph by default.
[info]
app_id = "sqlitebrowser"
tier = "third-party"

[filesystem]
documents = true
