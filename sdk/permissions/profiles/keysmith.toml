# Starting permission profile for Keysmith (a KDE OTP two-factor code app). Keeps
# its secrets in its own config dir; no file access, no network and no Knowledge
# Graph by default.
[info]
app_id = "keysmith"
tier = "third-party"
