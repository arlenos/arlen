# Starting permission profile for KDESvn (a KDE Subversion client). Works on local
# checkouts in Documents and talks to SVN servers over the network. No Knowledge
# Graph access.
[info]
app_id = "org.kde.kdesvn"
tier = "third-party"

[filesystem]
documents = true

[network]
allow_all = true
