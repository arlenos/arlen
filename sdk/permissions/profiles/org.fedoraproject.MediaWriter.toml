# Starting permission profile for Fedora Media Writer.
# It downloads OS images over the network and writes them to removable media by function. It gets network and Downloads. No Knowledge Graph access.
[info]
app_id = "org.fedoraproject.MediaWriter"
tier = "third-party"

[filesystem]
downloads = true

[network]
allow_all = true
