[package]
license = "AGPL-3.0-only"
name = "arlen-confiner"
version = "0.1.0"
edition = "2021"
description = "Shared unprivileged (bubblewrap) confiner for Arlen: one base, a forage build profile and an arlen-run app-runtime profile"

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = "2"

[dev-dependencies]
tempfile = "3"
