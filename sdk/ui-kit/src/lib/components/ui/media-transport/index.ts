/// The shared media transport cluster (previous / play-pause / next), used by
/// every player surface so the controls read identically.
export { default as MediaTransport } from "./media-transport.svelte";
