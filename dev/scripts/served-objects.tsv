# The bus surfaces this tree owns, and the object path each one must serve.
#
# binary <TAB> bus name <TAB> object path
#
# Fed to `dev/scripts/probe-served-objects.sh --all dev/scripts/served-objects.tsv`,
# which starts each daemon on a private bus and asks what it actually serves.
#
# HAND-KEPT, and that is a stated weakness rather than a design. The pairs belong
# to the call sites - the serving side names them, the calling side dials them -
# but deriving them is not a grep: ownership uses two idioms in this tree
# (`request_name` in three daemons, `connection::Builder::name` in the rest), the
# name and path are usually constants resolved per crate, and a scan by string
# SHAPE over-collects badly. `org.arlen.Transfer1` looks exactly like a bus name
# and is documented as an identifier the transfer daemon deliberately does not
# own, so a shape-built list would report correct code as broken every run.
# Deriving this properly belongs with the boot-verify job.
#
# Every `BusName=` in a shipped unit must appear here, as a pair or as an
# `!exclude` line carrying the reason it cannot be one. `check-bus-names-covered.py`
# holds this file to that, so a new daemon cannot land its unit and leave this
# gate quietly covering less than it did the day before.
#
# `!exclude <name> <reason>` - declared, but not probeable the way this probe
# works. Not a way to make a name go away: the reason is read by a person.
#
# A binary that is not built is SKIPPED by the sweep rather than failing it, so a
# pair may name a daemon this checkout has not compiled.

!exclude	org.arlen.Transfer1	a reserved identifier, not a name anyone takes (daemons/transfer-daemon/src/request_socket.rs:30)
!exclude	org.arlen.InstallHelper1	root-owned on the SYSTEM bus; this probe drives a private session bus as the developer's own uid
!exclude	org.arlen.PermissionHelper1	root-owned on the SYSTEM bus, same as the install helper
!exclude	org.freedesktop.impl.portal.desktop.arlen	an impl-portal BACKEND: the frontend dispatches to it, so it is reached through xdg-desktop-portal rather than dialled directly

arlen-ai-engine-daemon	org.arlen.AI1	/org/arlen/AI1
arlen-ai-engine-daemon	org.arlen.AIAgent1	/org/arlen/AIAgent1
arlen-clockd	org.arlen.Clock1	/org/arlen/Clock1
arlen-powerd	org.arlen.Power1	/org/arlen/Power1
arlen-undod	org.arlen.Undo1	/org/arlen/Undo1
arlen-connectionsd	org.arlen.Connections1	/org/arlen/Connections1
arlen-accountsd	org.arlen.Accounts1	/org/arlen/Accounts1
arlen-installd	org.arlen.InstallDaemon1	/org/arlen/InstallDaemon1
arlen-notifyd	org.freedesktop.Notifications	/org/freedesktop/Notifications
