# Boot-verify harness (TIER-A 1b): expose QEMU fw_cfg entries under
# /sys/firmware/qemu_fw_cfg so arlen-session can read opt/arlen/verify_app and
# launch a daily-driver app for dev/vm/verify.py to screenshot. Harmless on real
# hardware (no fw_cfg device -> the module loads but exposes nothing).
qemu_fw_cfg
