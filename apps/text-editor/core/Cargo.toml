[package]
license = "AGPL-3.0-only"
name = "arlen-text-editor-core"
version = "0.1.0"
edition = "2021"
description = "Arlen text editor: the pure LSP-client core (framing, handshake, document sync, diagnostics) the Tauri host drives a language server with."
authors = ["Arlen"]

[lib]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
