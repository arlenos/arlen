[package]
license = "AGPL-3.0-only"
name = "arlen-terminal-core"
version = "0.1.0"
edition = "2021"
description = "Arlen terminal: the embeddable core contract types (Block, Session, the terminal IPC shapes)"
authors = ["Arlen"]

[lib]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
