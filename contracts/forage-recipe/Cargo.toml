[package]
name = "arlen-forage-recipe"
version = "0.1.0"
edition = "2021"
description = "Parser and validator for the forage recipe.toml format (section 5a schema)"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "2"

[workspace]
