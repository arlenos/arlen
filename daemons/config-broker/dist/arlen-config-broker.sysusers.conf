# The dedicated uid that OWNS the AI master switches.
#
# The whole point of the config broker is that the session user cannot write
# this state directly, so it needs an account of its own. A system user with no
# shell and no home: it never logs in, it only runs the broker.
u arlen-config - "Arlen config broker" - /usr/sbin/nologin
